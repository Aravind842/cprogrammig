#include<stdio.h>
int main()
{
	int a[10];
	int n,i,maxa=a[0];
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		scanf("%d",&a[i]);
		if(maxa<a[i])
		{
			maxa=a[i];
		}
	}
	
	printf("\t%d",maxa);
	return 0;
}
